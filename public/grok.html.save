<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Win a $1,000 Visa Gift Card!</title>
  <meta name="robots" content="noindex"> <!-- Block search engine bots -->
  <style>
    body { font-family: Arial, sans-serif; text-align: center; background: #f4f4f4; margin: 0; }
    .container { max-width: 600px; margin: 50px auto; padding: 20px; background: #fff; border-radius: 10px; }
    h1 { color: #d32f2f; }
    .cta-btn { background: #0288d1; color: #fff; padding: 15px 30px; text-decoration: none; border-radius: 5px; font-size: 18px; }
    .cta-btn:hover { background: #0277bd; }
    .timer { color: #d32f2f; font-weight: bold; }
    .hidden { display: none; } /* For cloaking */
  </style>
</head>
<body>
  <div class="container">
    <h1 id="headline">Win a $1,000 Visa Gift Card!</h1>
    <p>Hurry, only a few spots left!</p>
    <p class="timer">Ends in <span id="countdown">10:00</span></p>
    <a href="#" id="cta-link" class="cta-btn">Claim Now!</a>
    <div id="bot-message" class="hidden">Welcome, please verify your details.</div> <!-- Shown to bots -->
  </div>
  <script>
    // Basic cloaking: Show different content to bots vs. real users
    const isBot = /bot|googlebot|crawler|spider|robot|crawling/i.test(navigator.userAgent);
    const headline = document.getElementById("headline");
    const ctaLink = document.getElementById("cta-link");
    const botMessage = document.getElementById("bot-message");
    
    if (isBot) {
      headline.textContent = "Welcome to Our Sweepstakes Page";
      ctaLink.classList.add("hidden");
      botMessage.classList.remove("hidden");
    } else {
      ctaLink.href = "YOUR_AFFILIATE_LINK"; // Replace with your link
    }

    // Countdown timer for urgency
    let time = 600; // 10 mins
    const countdown = document.getElementById("countdown");
    setInterval(() => {
      let mins = Math.floor(time / 60);
      let secs = time % 60;
      countdown.textContent = `${mins}:${secs < 10 ? "0" : ""}${secs}`;
      time--;
      if (time < 0) time = 600;
    }, 100efault();
      const re
